# Google Apps Script Web App URL for saving Book Demo form submissions to Google Sheets.
# Steps to set up:
# 1. Open your Google Sheet and go to Extensions → Apps Script.
# 2. Paste the following script and deploy it as a Web App (Execute as: Me, Who has access: Anyone):
#
#    function doPost(e) {
#      var sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
#      var data = JSON.parse(e.postData.contents);
#      sheet.appendRow([
#        data.submittedAt,
#        data.name,
#        data.email,
#        data.company,
#        data.phone,
#        data.additionalInfo
#      ]);
#      return ContentService.createTextOutput(JSON.stringify({ status: "ok" }))
#        .setMimeType(ContentService.MimeType.JSON);
#    }
#
# 3. Copy the deployed Web App URL and paste it below.
VITE_GOOGLE_SHEET_WEBHOOK_URL=
